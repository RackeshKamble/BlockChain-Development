//React Props used 
//Used to show connect to metamask messages
const Banner = ({ text }) => {
    return (
      <div className='banner'>
        <h1>{text}</h1>
      </div>
    );
  }
  
  export default Banner;